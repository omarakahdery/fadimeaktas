export function Favorites() {
  return (
    <main className="content-wrapper">
      <h1 className="h3 container pb-3 pb-lg-4">Favorilerim</h1>
      <section className="container">

     {/*   <div className="row gy-4 gy-md-5 pb-4 pb-md-5">

          {imges.filter(i => i.id == 0).map((src, index) => (
            <Item key={src.src} src={src} name={src.name} id={String(src.id)}/>
          ))}

        </div>*/}
      </section>
    </main>
  )
}